import { convertSecondsToTimeUnits } from '../index';

describe('index.ts', () => {
    describe('convertSecondsToTimeUnits', () => {
        describe('example tests', () => {
            it('should be a function', () => {
                expect(typeof convertSecondsToTimeUnits).toBe('function');
            });
        });
    });
});
